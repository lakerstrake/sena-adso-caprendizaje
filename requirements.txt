# Dependencias del pipeline ETL y de build del directorio SENA ADSO.
# La cache de pip en GitHub Actions (setup-python cache: 'pip') exige este
# archivo; sin el, el workflow de sincronizacion aborta antes de ejecutarse.
requests>=2.31,<3
beautifulsoup4>=4.12,<5
pandas>=2.1,<3
urllib3>=2.0,<3
openpyxl>=3.1,<4
